contents:
    repositories:
        - https://packages.wolfi.dev/os
    keyring:
        - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
        - wolfi-baselayout
        - ca-certificates-bundle
        - dns01proxy

entrypoint:
    command: /usr/bin/dns01proxy

archs:
    - x86_64
    - aarch64
